<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="../../main.css">
    <link rel="stylesheet" href="../../components/form/groupItem.css">
    <link rel="stylesheet" href="../../components/buttons/primaryButton.css">
    <link rel="stylesheet" href="../../components/buttons/linkButton.css">
    <link rel="stylesheet" href="authorization.css">
    <title>{{pages.signup.title}}</title>
</head>
<body>
    <div class="container">
        <div class="container__title">
            <span>{{pages.signup.title}}</span>
        </div>
        <form id="signup-form" action="" class="container__form">
            <div class="container__fields">
                {{> form/groupItem id="email" name="email" label="Почта" value=""}}
                {{> form/groupItem id="login" name="login" label="Логин" value=""}}
                {{> form/groupItem id="first_name" name="first_name" label="Имя" value=""}}
                {{> form/groupItem id="second_name" name="second_name" label="Фамилия" pvalue=""}}
                {{> form/groupItem id="phone" name="phone" label="Телефон" value=""}}
                {{> form/groupItem id="password" name="password" label="Пароль" type="password" value=""}}
                {{> form/groupItem id="confirm_password" name="confirm_password" label="Пароль (еще раз)" type="password" value=""}}
            </div>
            <div class="container__buttons">
                {{> buttons/primaryButton id="submit" form="signup-form" type="submit" text="Зарегистрироваться"}}
                {{> buttons/linkButton href=pages.signin.href text="Войти"}}
            </div>
        </form>
    </div>
</body>
</html>